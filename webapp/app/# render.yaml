# render.yaml
services:
  - type: web
    name: traphouse-bot
    env: node
    buildCommand: npm install
    startCommand: node index.js
    
  - type: web  
    name: degens-bot
    env: node
    buildCommand: npm install
    startCommand: node index.js